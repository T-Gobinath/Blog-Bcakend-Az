# Server port: use hosting platform PORT. Locally falls back to 8080.
server.port=${PORT:8081}

# Datasource: JDBC URL and credentials.
# These defaults are set from the external Render DB info you previously provided.
# For security, consider removing defaults and setting the SPRING_DATASOURCE_* vars in your host instead.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d423akbipnbc73c0ijag-a.oregon-postgres.render.com:5432/myapp_db_0k76?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:myapp_db_0k76_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:I7mNgmN3ESTawc8MINK9HpDzn5huF7xs}

# Driver and JPA settings
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}

# Connection pool sensible defaults (optional)
spring.datasource.hikari.maximum-pool-size=${SPRING_HIKARI_MAX:10}
spring.datasource.hikari.minimum-idle=${SPRING_HIKARI_MIN:2}
spring.datasource.hikari.idle-timeout=${SPRING_HIKARI_IDLE_MS:300000}